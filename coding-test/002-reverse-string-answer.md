# 코딩 테스트 #2: 문자열 뒤집기 - 정답 및 해설

**날짜**: 2025-11-01

## 당신의 풀이

```typescript
function solution(s: string[]): string[] {
  let left = 0;
  let right = s.length - 1;

  while (left < right) {
    let temp = s[left];
    s[left] = s[right];
    s[right] = temp;
    left++;
    right--;
  }

  return s;
}
```

**결과**: ✅ 모든 테스트 통과!

## 코드 분석

### 접근 방법: Two Pointers (투 포인터)

당신의 풀이는 **투 포인터 기법**을 사용한 최적의 솔루션입니다! 👍

**핵심 아이디어:**
- 배열의 양 끝에서 시작하는 두 개의 포인터 사용
- 두 포인터가 가리키는 요소를 교환
- 포인터들을 중앙으로 이동
- 두 포인터가 만날 때까지 반복

**시간 복잡도:** O(n)
- 배열의 절반만 순회 (n/2)
- 각 요소는 한 번만 처리

**공간 복잡도:** O(1)
- temp 변수 하나만 사용
- 추가 배열 생성 없음

### 동작 원리

```
초기 상태: ["h", "e", "l", "l", "o"]
            ↑                   ↑
          left                right

1단계: "h"와 "o" 교환
       ["o", "e", "l", "l", "h"]
             ↑           ↑
           left        right

2단계: "e"와 "l" 교환
       ["o", "l", "l", "e", "h"]
                  ↑
            left, right (중앙에서 만남)

완료!
```

## 다른 풀이 방법들

### 방법 1: JavaScript 내장 메서드 (reverse)

```typescript
function solutionBuiltIn(s: string[]): string[] {
  return s.reverse();
}
```

**장점:**
- 가장 간단하고 직관적
- 코드 한 줄

**단점:**
- 알고리즘 이해도를 보여주기 어려움
- 코딩 테스트에서는 직접 구현을 요구할 수 있음

**시간/공간 복잡도:** O(n) / O(1)

### 방법 2: 재귀 (Recursion)

```typescript
function solutionRecursive(s: string[]): string[] {
  function reverse(left: number, right: number): void {
    if (left >= right) return;

    // 교환
    [s[left], s[right]] = [s[right], s[left]];

    // 재귀 호출
    reverse(left + 1, right - 1);
  }

  reverse(0, s.length - 1);
  return s;
}
```

**장점:**
- 우아한 코드
- 재귀 연습에 좋음

**단점:**
- 스택 공간 사용 O(n)
- 큰 배열에서 스택 오버플로우 위험

**시간/공간 복잡도:** O(n) / O(n) (재귀 스택)

### 방법 3: 구조 분해 할당 (Destructuring)

```typescript
function solutionDestructuring(s: string[]): string[] {
  let left = 0;
  let right = s.length - 1;

  while (left < right) {
    // temp 변수 없이 교환
    [s[left], s[right]] = [s[right], s[left]];
    left++;
    right--;
  }

  return s;
}
```

**특징:**
- 당신의 풀이와 거의 동일
- JavaScript/TypeScript의 구조 분해 할당 활용
- temp 변수 불필요

## 풀이 비교

| 방법 | 시간 | 공간 | 가독성 | 면접 적합도 |
|------|------|------|--------|------------|
| Two Pointers (당신의 풀이) | O(n) | O(1) | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 내장 reverse() | O(n) | O(1) | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| 재귀 | O(n) | O(n) | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| 구조 분해 할당 | O(n) | O(1) | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

## 학습 포인트

### 1. Two Pointers 패턴

투 포인터는 배열/문자열 문제에서 매우 자주 사용되는 패턴입니다.

**언제 사용?**
- 정렬된 배열에서 특정 조건 찾기
- 배열 양 끝에서 중앙으로 이동
- In-place 수정이 필요할 때

**비슷한 문제들:**
- 회문(Palindrome) 검사
- Container With Most Water
- Remove Duplicates from Sorted Array

### 2. In-Place 알고리즘

**In-Place란?**
- 추가 배열을 생성하지 않고 원본을 직접 수정
- 공간 복잡도 O(1)
- 메모리 효율적

**주의사항:**
- 원본 데이터가 변경됨
- 필요하면 복사본 만들어서 작업

### 3. 변수 교환 방법

```typescript
// 방법 1: temp 변수 (당신의 방법)
let temp = a;
a = b;
b = temp;

// 방법 2: 구조 분해 할당 (현대적)
[a, b] = [b, a];

// 방법 3: 산술 연산 (정수만, 권장하지 않음)
a = a + b;
b = a - b;
a = a - b;

// 방법 4: XOR (정수만, 권장하지 않음)
a = a ^ b;
b = a ^ b;
a = a ^ b;
```

## 코드 개선 제안

당신의 코드는 이미 완벽하지만, TypeScript 스타일로 약간 개선할 수 있습니다:

```typescript
function solution(s: string[]): string[] {
  let left = 0;
  let right = s.length - 1;

  while (left < right) {
    // 구조 분해 할당 사용
    [s[left], s[right]] = [s[right], s[left]];
    left++;
    right--;
  }

  return s;
}
```

또는 for문 스타일:

```typescript
function solution(s: string[]): string[] {
  const len = s.length;

  for (let i = 0; i < len / 2; i++) {
    [s[i], s[len - 1 - i]] = [s[len - 1 - i], s[i]];
  }

  return s;
}
```

## 실전 팁

1. **문제를 잘 읽기**
   - "in-place" 요구사항을 놓치지 말 것
   - 공간 복잡도 제약 확인

2. **엣지 케이스 고려**
   - 빈 배열: `[]`
   - 단일 요소: `["A"]`
   - 두 요소: `["a", "b"]`

3. **테스트 케이스 작성**
   - 홀수 길이 배열
   - 짝수 길이 배열
   - 경계 조건

---

**총평**: 완벽한 Two Pointers 구현입니다! 🎉

이 문제는 투 포인터 패턴의 가장 기본적인 예시로,
앞으로 더 복잡한 문제를 푸는 데 기반이 됩니다.
매우 깔끔하고 효율적인 코드를 작성하셨습니다!
