# CS 기초 질문 #4: HTTP와 HTTPS - 정답

**날짜**: 2025-11-04

## 정답 및 설명

### HTTP (HyperText Transfer Protocol)

**정의:**
- 클라이언트(브라우저)와 서버 간에 데이터를 주고받기 위한 통신 규약(프로토콜)
- 웹에서 HTML, 이미지, 동영상 등의 데이터를 전송하는 기본 프로토콜

**특징:**
- **암호화되지 않음** - 평문(Plain Text)으로 데이터 전송
- 포트 번호: 80
- 빠르지만 보안에 취약

### HTTPS (HyperText Transfer Protocol Secure)

**정의:**
- HTTP에 **보안(Secure) 계층**을 추가한 프로토콜
- SSL/TLS 암호화를 사용하여 데이터를 안전하게 전송

**특징:**
- **암호화됨** - 데이터가 암호화되어 전송
- 포트 번호: 443
- HTTP보다 약간 느리지만 안전함
- 브라우저 주소창에 자물쇠 아이콘 표시

## HTTP vs HTTPS 비교

### 핵심 차이점

| 항목 | HTTP | HTTPS |
|------|------|-------|
| **보안** | 암호화 없음 | SSL/TLS 암호화 |
| **포트** | 80 | 443 |
| **속도** | 빠름 | 약간 느림 (암호화 오버헤드) |
| **인증서** | 불필요 | SSL 인증서 필요 |
| **데이터 보호** | 없음 | 있음 |
| **브라우저 표시** | 안전하지 않음 경고 | 자물쇠 아이콘 |
| **SEO** | 불리 | 유리 |

### 시각적 비교

```
HTTP 통신:
┌─────────┐                              ┌─────────┐
│ 브라우저 │ ──── "비밀번호: 1234" ────→ │  서버   │
└─────────┘      (평문으로 전송!)        └─────────┘
      ↑                                        │
      └────────── 해커가 가로채기 쉬움! ───────┘


HTTPS 통신:
┌─────────┐                              ┌─────────┐
│ 브라우저 │ ── "Xk9#mP$2..." (암호화) → │  서버   │
└─────────┘      (해독 불가!)            └─────────┘
      ↑                                        │
      └────────── 해커가 가로채도 못 읽음 ─────┘
```

## HTTPS가 더 안전한 이유

### 1. 데이터 암호화 (Encryption)

```
HTTP:
사용자 → [아이디: user@email.com] → 서버
         [비밀번호: password123]
         ↑ 누구나 읽을 수 있음!

HTTPS:
사용자 → [Xk#9mP$2nQ!] → 서버
         ↑ 암호화된 데이터, 서버만 해독 가능
```

**암호화 과정:**
1. 브라우저가 서버에 연결 요청
2. 서버가 SSL 인증서 전달
3. 브라우저가 인증서 검증
4. 암호화 키 생성 및 교환
5. 이후 모든 통신이 암호화됨

### 2. 데이터 무결성 (Integrity)

```
HTTP:
사용자 → [계좌이체: 10만원] → (해커 변조) → [계좌이체: 100만원] → 서버
         ↑ 중간에 데이터 변조 가능!

HTTPS:
사용자 → [암호화된 데이터 + 체크섬] → 서버
         ↑ 변조 시 체크섬 불일치로 감지
```

**체크섬(Checksum):**
- 데이터가 변조되지 않았는지 확인하는 값
- 데이터가 조금만 바뀌어도 체크섬이 완전히 달라짐

### 3. 신원 확인 (Authentication)

```
HTTP:
사용자 → 어떤 서버인지 확인 불가 → 가짜 서버일 수도!

HTTPS:
사용자 → SSL 인증서 확인 → 진짜 은행 사이트 확인 ✓
```

**SSL 인증서:**
- 신뢰할 수 있는 기관(CA)이 발급
- 서버의 신원을 보증
- 유효기간이 있음

## 실제 예시

### 예시 1: 로그인 정보 전송

**HTTP (위험):**
```
POST http://example.com/login
Content-Type: application/json

{
  "email": "user@email.com",
  "password": "mypassword123"
}

→ 누군가 네트워크를 감청하면 그대로 읽힘!
→ 공공 와이파이에서 특히 위험!
```

**HTTPS (안전):**
```
POST https://example.com/login
Content-Type: application/json

{
  "email": "user@email.com",
  "password": "mypassword123"
}

→ 암호화되어 전송: "Xk9#mP$2nQ!8vR..."
→ 서버만 해독 가능!
```

### 예시 2: 신용카드 정보 입력

**HTTP 사이트:**
```
❌ 안전하지 않음
http://shopping-site.com

신용카드 번호: 1234-5678-9012-3456
→ 평문으로 전송 → 해커가 가로챌 수 있음!
```

**HTTPS 사이트:**
```
✅ 안전함
https://shopping-site.com
🔒 (자물쇠 아이콘)

신용카드 번호: 1234-5678-9012-3456
→ 암호화되어 전송 → 안전!
```

## 브라우저에서 확인하는 방법

### Chrome/Firefox/Safari에서:

```
HTTP 사이트:
┌──────────────────────────────────────┐
│ ⚠️ 안전하지 않음 | http://example.com │
└──────────────────────────────────────┘

HTTPS 사이트:
┌──────────────────────────────────────┐
│ 🔒 | https://example.com              │
└──────────────────────────────────────┘
```

**자물쇠 아이콘 클릭 시:**
- 인증서 정보
- 연결 보안 상태
- 쿠키 및 사이트 데이터
- 권한

## SSL/TLS 프로토콜

### SSL (Secure Sockets Layer)
- 1990년대 Netscape가 개발
- 현재는 사용 중단 (보안 취약점)

### TLS (Transport Layer Security)
- SSL의 후속 버전
- 현재 표준: TLS 1.2, TLS 1.3
- HTTPS는 실제로 TLS를 사용하지만 관습적으로 "SSL"이라고도 부름

### 암호화 과정 (TLS Handshake)

```
1. Client Hello
   브라우저 → 서버
   "안녕! TLS 1.3 지원해요. 이런 암호화 방식 가능해요."

2. Server Hello
   서버 → 브라우저
   "안녕! TLS 1.3 사용할게요. 이 방식으로 암호화하죠."
   + SSL 인증서 전송

3. 인증서 검증
   브라우저가 인증서 확인
   - 유효한 CA가 발급했나?
   - 유효기간이 지나지 않았나?
   - 도메인이 일치하나?

4. 세션 키 생성
   브라우저 ↔ 서버
   안전하게 암호화 키 교환

5. 암호화 통신 시작
   이후 모든 데이터가 암호화됨
```

## 프론트엔드 개발자가 알아야 할 내용

### 1. Mixed Content 경고

```javascript
// HTTPS 페이지에서 HTTP 리소스 로드 ❌
<img src="http://example.com/image.jpg">
<script src="http://example.com/script.js"></script>

// 브라우저 경고:
// "Mixed Content: This page was loaded over HTTPS,
//  but requested an insecure resource."

// 해결책: HTTPS 사용 ✅
<img src="https://example.com/image.jpg">
<script src="https://example.com/script.js"></script>

// 또는 프로토콜 상대 URL 사용
<img src="//example.com/image.jpg">
```

### 2. Secure Cookie

```javascript
// HTTP에서는 쿠키가 평문으로 전송됨 ❌
document.cookie = "sessionId=abc123";

// HTTPS에서 Secure 플래그 사용 ✅
document.cookie = "sessionId=abc123; Secure; HttpOnly";
// Secure: HTTPS에서만 전송
// HttpOnly: JavaScript로 접근 불가 (XSS 방지)
```

### 3. API 호출

```javascript
// HTTP API 호출 ❌ (HTTPS 페이지에서)
fetch('http://api.example.com/data')
  .then(res => res.json())
  // Mixed Content 오류 발생!

// HTTPS API 호출 ✅
fetch('https://api.example.com/data')
  .then(res => res.json())
  .then(data => console.log(data));
```

### 4. 리다이렉트 처리

```javascript
// HTTP를 HTTPS로 자동 리다이렉트
if (location.protocol !== 'https:') {
  location.replace(`https:${location.href.substring(location.protocol.length)}`);
}

// 하지만 서버에서 처리하는 게 더 좋음 (301 리다이렉트)
```

## HTTPS 적용 방법

### 1. SSL 인증서 발급

**무료 옵션:**
- **Let's Encrypt** (가장 인기)
  - 완전 무료
  - 3개월마다 갱신 (자동화 가능)
  - certbot 도구 사용

**유료 옵션:**
- DigiCert, GoDaddy, Comodo 등
- 1년 단위 구매
- 조직 인증(OV), 확장 인증(EV) 등 고급 옵션

### 2. 서버 설정 (예: Nginx)

```nginx
server {
    listen 80;
    server_name example.com;

    # HTTP를 HTTPS로 리다이렉트
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name example.com;

    # SSL 인증서 경로
    ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;

    # 보안 설정
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # HSTS 활성화
    add_header Strict-Transport-Security "max-age=31536000" always;

    location / {
        # 앱 설정
    }
}
```

### 3. 클라우드 서비스 이용

**Vercel, Netlify, Cloudflare Pages:**
```bash
# 자동으로 HTTPS 적용!
# 배포만 하면 끝

vercel deploy
# 또는
netlify deploy
```

## HTTPS의 추가 이점

### 1. SEO (검색 엔진 최적화)

```
Google 검색 순위 요소:
✅ HTTPS 사이트: +순위 보너스
❌ HTTP 사이트: -순위 페널티

2014년부터 Google이 공식 발표
```

### 2. 브라우저 기능 접근

```javascript
// 다음 기능들은 HTTPS에서만 동작:

// Geolocation API
navigator.geolocation.getCurrentPosition()
// HTTP: 차단됨 ❌
// HTTPS: 동작 ✅

// Service Worker (PWA)
navigator.serviceWorker.register('/sw.js')
// HTTP: 차단됨 ❌ (localhost 제외)
// HTTPS: 동작 ✅

// Camera/Microphone
navigator.mediaDevices.getUserMedia({ video: true })
// HTTP: 차단됨 ❌
// HTTPS: 동작 ✅

// Notifications API
Notification.requestPermission()
// HTTP: 차단됨 ❌
// HTTPS: 동작 ✅
```

### 3. 사용자 신뢰

```
사용자가 보는 것:

HTTP 사이트:
"⚠️ 안전하지 않음"
→ 사용자가 떠날 가능성 높음

HTTPS 사이트:
"🔒 안전함"
→ 사용자가 신뢰하고 정보 입력
```

### 4. HTTP/2 지원

```
HTTP/2 장점:
- 멀티플렉싱 (여러 요청 동시 처리)
- 헤더 압축
- 서버 푸시

대부분의 브라우저:
HTTP/2 = HTTPS 필수!
```

## 보안 관련 추가 팁

### 1. HSTS (HTTP Strict Transport Security)

```javascript
// 서버 응답 헤더:
Strict-Transport-Security: max-age=31536000; includeSubDomains

// 의미:
// "앞으로 1년간 이 사이트는 무조건 HTTPS로만 접속하세요"
// HTTP 입력해도 브라우저가 자동으로 HTTPS로 변경
```

### 2. CSP (Content Security Policy)

```html
<!-- HTTPS 리소스만 허용 -->
<meta http-equiv="Content-Security-Policy"
      content="upgrade-insecure-requests">
```

### 3. 인증서 모니터링

```bash
# 인증서 만료일 확인
openssl s_client -connect example.com:443 | openssl x509 -noout -dates

# 만료 전 자동 갱신 설정 (Let's Encrypt)
certbot renew --dry-run
```

## 흔한 오해와 진실

### 오해 1: "내 사이트는 작아서 HTTPS 필요 없어"
❌ **틀림**
- 모든 웹사이트는 HTTPS를 사용해야 함
- Google, 브라우저들이 HTTP 사이트에 경고 표시
- SEO에도 불리

### 오해 2: "HTTPS는 비싸다"
❌ **틀림**
- Let's Encrypt로 완전 무료
- Vercel, Netlify 등은 자동으로 제공

### 오해 3: "HTTPS는 느리다"
⚠️ **예전엔 맞았지만 지금은 아님**
- 현대 하드웨어에서 암호화 오버헤드는 미미
- HTTP/2 + HTTPS가 HTTP/1.1보다 빠름
- 차이를 느낄 수 없는 수준

### 오해 4: "HTTPS면 100% 안전하다"
❌ **틀림**
- HTTPS는 전송 구간만 보호
- 서버 해킹, SQL Injection 등은 별개
- HTTPS + 기타 보안 조치 필요

## 정리

### HTTP
```
✅ 장점:
- 설정 간단
- 약간 더 빠름 (미미한 차이)

❌ 단점:
- 보안 취약
- 브라우저 경고
- SEO 불리
- 최신 기능 사용 불가
```

### HTTPS
```
✅ 장점:
- 데이터 암호화 (보안)
- 데이터 무결성
- 신원 확인
- SEO 유리
- 사용자 신뢰
- 최신 브라우저 기능 사용 가능

❌ 단점:
- 약간의 성능 오버헤드 (무시할 수준)
- 인증서 관리 필요 (자동화 가능)
```

**결론: 모든 웹사이트는 HTTPS를 사용해야 합니다!**

---

**요약:**

**HTTP와 HTTPS의 차이:**
- HTTP: 평문 통신 (암호화 없음)
- HTTPS: 암호화 통신 (SSL/TLS)

**HTTPS가 더 안전한 이유:**
1. **암호화**: 데이터를 가로채도 읽을 수 없음
2. **무결성**: 데이터 변조 방지
3. **인증**: 서버 신원 확인

**프론트엔드 개발자 체크리스트:**
- ✅ 모든 리소스 HTTPS로 로드
- ✅ API도 HTTPS 사용
- ✅ Secure 쿠키 설정
- ✅ Mixed Content 경고 해결
- ✅ HSTS 헤더 설정

**핵심:**
웹 개발을 한다면 HTTPS는 선택이 아닌 필수입니다!
